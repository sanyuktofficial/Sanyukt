# Copy this file to .env and fill in your values.
# Do not commit .env (it contains secrets).

# MongoDB Atlas connection string (required for production)
MONGO_URI=mongodb+srv://sanyuktofficial_db_user:nbdhCgY64GArFojR@cluster0.0nkb8kt.mongodb.net/sanyukt?retryWrites=true&w=majority&appName=Cluster0

# Server port (default: 3000)
PORT=3000

# JWT secret for signing tokens (use a long random string in production)
JWT_SECRET=change-me-in-production

# Path to Firebase service account JSON (for verifying Google ID tokens)
FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-service-account.json
